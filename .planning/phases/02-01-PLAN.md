# Plan 02-01: Document views (INFO / PARTS / INSTRUCTIONS)

**Phase**: 2 | **Requirements**: REQ-04, REQ-05, REQ-08 | **Executor**: fresh-context implementer subagent (dispatched 2026-08-29)

Owns: src/components/tabs/{InfoTab,PartsTab,InstructionsTab}.tsx, src/lib/progress.ts, tab-local subcomponents.
- INFO: cover-render panel (glyph + gradients + "AI RENDER deferred" caption), tag chips, AI summary, title/author, collapsible BOM rollup (domain → category → parts) with costs + total, via bomRollup/totalCost/fmtCost.
- PARTS: search + category filter, thumbed rows (name/role/description/printSettings/category chip), qty/unit/subtotal columns, sticky total footer.
- INSTRUCTIONS: done counter, Tools & Assumptions card, numbered phases with per-phase counters, steps with checkbox + tool/part chips + collapsible detail; progress persisted at obp:progress:{slug}.
Must-haves: renders any DesignPackage; tsc clean; checkbox state survives reload.
