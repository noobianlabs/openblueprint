# Plan 03-01: Diagram views (WIRING / MECH)

**Phase**: 3 | **Requirements**: REQ-06, REQ-07 | **Executor**: fresh-context implementer subagent (dispatched 2026-08-29)

Owns: src/components/tabs/{WiringTab,MechTab}.tsx + wiring//mech/ subcomponents.
- WIRING: @xyflow/react graph; custom part nodes (category header, role/name, per-pin handles); deterministic category-column layout (power→sensor→mcu→module→actuator/display); edges styled per net (data solid/animated, power dashed, ground dotted) with labels; parts sidebar with click-to-center; DATA/POWER/GROUND legend; controls; fitView.
- MECH: blueprint-grid viewer placeholder ("CAD generation deferred in v1") + recursive assembly tree sidebar with category dots, selection detail card, category count chips.
Must-haves: all 21 exemplar connections render attached to real pins; tree mirrors assembly data; tsc clean.
