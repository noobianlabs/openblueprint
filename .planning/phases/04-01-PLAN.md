# Plan 04-01: Generation & persistence

**Phase**: 4 | **Requirements**: REQ-09, REQ-10, REQ-11 | **Executor**: fresh-context implementer subagent (dispatched 2026-08-29); star/copy wiring in ProjectView lands in the orchestrator integration pass

Owns: src/lib/engine/* (types, local archetype engine + part library + validate, claude client), src/lib/store.ts, /api/generate/{plan,build} routes, /new run view, /projects, /about, /p/[slug] resolution.
- Engine contract: plan(prompt) → {decisions, questions}; build(prompt, answers) → DesignPackage. Local engine: prompt-hash-seeded deterministic archetype builders over a realistic part library; refine answers swap real parts. Claude engine (claude-api skill; @anthropic-ai/sdk; forced tool_use against the DesignPackage JSON schema) behind ANTHROPIC_API_KEY; both outputs pass the same referential-integrity validator.
- Run view mirrors the researched product flow: staged checklist (planning → clarifying → electrical → mechanical → ready), streamed decision bullets, REFINE_DESIGN card with skip/generate, then save + redirect.
- Store: obp:projects / obp:stars, slug/cover generation. /projects: search, ALL|STARRED, sort, delete-with-confirm.
Must-haves: keyless prompt → viewable saved project; determinism (same prompt → same package); validator green on 8 prompts; tsc clean.
